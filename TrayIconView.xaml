<UserControl
    x:Class="UsbBridge.TrayIconView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:tb="using:H.NotifyIcon">
    <tb:TaskbarIcon
        x:Name="TrayIcon"
        x:FieldModifier="public"
        ToolTipText="WSL USB Bridge"
        ContextMenuMode="SecondWindow"
        DoubleClickCommand="{x:Bind ShowWindowCommand}"
        NoLeftClickDelay="True">
        <tb:TaskbarIcon.ContextFlyout>
            <MenuFlyout AreOpenCloseAnimationsEnabled="False">
                <MenuFlyoutItem Text="Show" Command="{x:Bind ShowWindowCommand}" />
                <MenuFlyoutSeparator />
                <MenuFlyoutItem Text="Exit" Command="{x:Bind ExitApplicationCommand}" />
            </MenuFlyout>
        </tb:TaskbarIcon.ContextFlyout>
    </tb:TaskbarIcon>
</UserControl>
